import 'package:flutter/material.dart';

class CategorieySection extends StatelessWidget {
  const CategorieySection({super.key});

  @override
  Widget build(BuildContext context) {
    return Center();
  }
}
